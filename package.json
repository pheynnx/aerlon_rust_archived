{
  "name": "tests",
  "scripts": {
    "dev": "run-p dev:*",
    "dev:css": "sass --no-source-map --watch sass:public/css",
    "dev:vite": "cd admin && pnpm build-watch",
    "dev:rust": "cargo run",
    "build": "run-s build:*",
    "build:css": "sass --no-source-map sass:public/css",
    "build:vite": "cd admin && pnpm build",
    "build:rust": "cargo run --release"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "npm-run-all": "^4.1.5",
    "sass": "^1.58.1"
  }
}
